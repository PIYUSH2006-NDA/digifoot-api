# ADD these to your existing requirements.txt
# (don't replace — append)

# reconstruction
open3d==0.18.0
xatlas==0.0.11
trimesh==4.4.0

open3d>=0.18.0
numpy>=1.24
torch>=2.0
# ML
torch==2.4.1
# pytorch3d: install via setup_deps.sh after torch

# infra (optional, for redis-backed job store)
redis==5.1.0
celery[redis]==5.4.0

# already likely have these but ensure versions:
fastapi>=0.110
uvicorn[standard]>=0.30
python-multipart>=0.0.9
numpy>=1.26
scipy>=1.13
opencv-python-headless>=4.10
